# 校园招聘系统前端接口文档

## 前端架构说明

本系统包含两个前端项目：
1. **管理端前端** (campus_manager-frontend) - 管理员和企业用户使用
2. **用户端前端** (campus-user-frontend) - 普通用户使用

## 管理端前端 (campus_manager-frontend)

### 技术栈
- Vue.js 2.6.14
- Element UI 2.15.6
- Vue Router 3.5.1
- Vuex 3.6.2
- Axios 0.24.0
- ECharts 6.0.0

### 项目结构
```
src/
├── api/                    # API接口封装
│   ├── company.js         # 企业相关接口
│   ├── employment.js      # 职位相关接口
│   ├── employmentUser.js  # 求职申请接口
│   ├── feedback.js        # 反馈接口
│   ├── login.js          # 登录接口
│   ├── resume.js         # 简历接口
│   └── user.js           # 用户接口
├── components/            # 公共组件
├── router/               # 路由配置
├── store/                # Vuex状态管理
├── styles/               # 样式文件
├── utils/                # 工具函数
└── views/                # 页面组件
```

### 路由配置

#### 管理员路由
```javascript
// 系统概览
{
  path: '/dashboard',
  component: Dashboard,
  meta: { title: '系统概览', role: 'admin' }
}

// 用户管理
{
  path: '/user',
  component: UserManagement,
  meta: { title: '用户管理', role: 'admin' }
}

// 公司管理
{
  path: '/company',
  component: CompanyManagement,
  meta: { title: '公司管理', role: 'admin' }
}

// 招聘职位管理
{
  path: '/employment',
  component: EmploymentManagement,
  meta: { title: '招聘职位管理', role: 'admin' }
}

// 简历管理
{
  path: '/resume',
  component: ResumeManagement,
  meta: { title: '简历管理', role: 'admin' }
}
```

#### 企业用户路由
```javascript
// 公司信息
{
  path: '/companyInfo',
  component: CompanyInfo,
  meta: { title: '公司信息', role: 'company' }
}

// 职位管理
{
  path: '/occupation',
  component: OccupationManagement,
  meta: { title: '职位管理', role: 'company' }
}

// 投递简历管理
{
  path: '/employmentUser',
  component: EmploymentUserManagement,
  meta: { title: '投递简历管理', role: 'company' }
}
```

### API接口封装

#### 用户接口 (api/user.js)
```javascript
import request from '@/utils/request'

// 获取用户信息
export function getUserInfo() {
  return request({
    url: '/user/username',
    method: 'get'
  })
}

// 分页查询用户
export function getUserList(data, pageNum, pageSize) {
  return request({
    url: `/user/${pageNum}/${pageSize}`,
    method: 'post',
    data
  })
}

// 更新用户信息
export function updateUser(data) {
  return request({
    url: '/user',
    method: 'put',
    data
  })
}

// 删除用户
export function deleteUser(id) {
  return request({
    url: `/user/${id}`,
    method: 'delete'
  })
}
```

#### 企业接口 (api/company.js)
```javascript
// 获取企业信息
export function getCompanyInfo() {
  return request({
    url: '/company/user/name',
    method: 'get'
  })
}

// 分页查询企业
export function getCompanyList(data, pageNum, pageSize) {
  return request({
    url: `/company/${pageNum}/${pageSize}`,
    method: 'post',
    data
  })
}

// 审核企业
export function auditCompany(data) {
  return request({
    url: '/company/audit',
    method: 'put',
    data
  })
}
```

#### 职位接口 (api/employment.js)
```javascript
// 获取职位列表
export function getEmploymentList(data, pageNum, pageSize) {
  return request({
    url: `/employment/${pageNum}/${pageSize}`,
    method: 'post',
    data
  })
}

// 获取企业职位
export function getCompanyEmployments(pageNum, pageSize) {
  return request({
    url: `/employment/username/${pageNum}/${pageSize}`,
    method: 'get'
  })
}

// 新增职位
export function addEmployment(data) {
  return request({
    url: '/employment',
    method: 'post',
    data
  })
}

// 审核职位
export function auditEmployment(data) {
  return request({
    url: '/employment/audit',
    method: 'put',
    data
  })
}
```

### 状态管理 (Vuex)

#### 用户状态 (store/user.js)
```javascript
const state = {
  userInfo: null,
  token: null,
  role: null
}

const mutations = {
  SET_USER_INFO(state, userInfo) {
    state.userInfo = userInfo
  },
  SET_TOKEN(state, token) {
    state.token = token
  },
  SET_ROLE(state, role) {
    state.role = role
  }
}

const actions = {
  // 登录
  login({ commit }, loginForm) {
    return new Promise((resolve, reject) => {
      login(loginForm).then(response => {
        const { data } = response
        commit('SET_TOKEN', data.token)
        commit('SET_USER_INFO', data.user)
        commit('SET_ROLE', data.user.role)
        resolve(data)
      }).catch(error => {
        reject(error)
      })
    })
  }
}
```

## 用户端前端 (campus-user-frontend)

### 技术栈
- Vue.js 2.6.14
- Element UI 2.15.6
- Vue Router 3.5.1
- Vuex 3.6.2
- Axios 0.24.0

### 项目结构
```
src/
├── api/                  # API接口
├── components/           # 公共组件
├── router/              # 路由配置
├── store/               # 状态管理
├── utils/               # 工具函数
└── views/               # 页面组件
    ├── company/         # 企业相关页面
    ├── Home.vue         # 首页
    ├── Jobs.vue         # 职位列表
    ├── JobDetail.vue    # 职位详情
    ├── Companies.vue    # 企业列表
    ├── Articles.vue     # 文章列表
    ├── Login.vue        # 登录页面
    ├── Register.vue     # 注册页面
    └── Profile.vue      # 个人中心
```

### 路由配置

#### 公共路由
```javascript
const routes = [
  { path: '/', redirect: '/home' },
  { path: '/home', component: Home },
  { path: '/jobs', component: Jobs },
  { path: '/job/:id', component: JobDetail },
  { path: '/companies', component: Companies },
  { path: '/articles', component: Articles },
  { path: '/login', component: Login },
  { path: '/register', component: Register }
]
```

#### 需要认证的路由
```javascript
// 个人中心
{
  path: '/profile',
  component: Profile,
  meta: { requiresAuth: true }
}
```

#### 企业用户路由
```javascript
// 企业注册
{
  path: '/company/register',
  component: CompanyRegister
}

// 企业控制台
{
  path: '/company/dashboard',
  component: CompanyDashboard,
  meta: { requiresAuth: true, requiresRole: 'Enterprise_User' }
}

// 企业职位管理
{
  path: '/company/jobs',
  component: CompanyJobs,
  meta: { requiresAuth: true, requiresRole: 'Enterprise_User' }
}

// 企业申请管理
{
  path: '/company/applications',
  component: CompanyApplications,
  meta: { requiresAuth: true, requiresRole: 'Enterprise_User' }
}
```

### 路由守卫
```javascript
router.beforeEach((to, from, next) => {
  // 检查是否需要认证
  if (to.matched.some(record => record.meta.requiresAuth)) {
    if (!isLoggedIn()) {
      next({
        path: '/login',
        query: { redirect: to.fullPath }
      })
      return
    }
    
    // 检查角色权限
    if (to.meta.requiresRole) {
      const userInfo = getUserInfo()
      if (!userInfo || userInfo.role !== to.meta.requiresRole) {
        next('/')
        return
      }
    }
  }
  
  next()
})
```

### API接口封装 (api/index.js)
```javascript
import axios from 'axios'

const api = axios.create({
  baseURL: 'http://localhost:3030',
  timeout: 10000
})

// 请求拦截器
api.interceptors.request.use(
  config => {
    const token = localStorage.getItem('token')
    if (token) {
      config.headers.Authorization = `Bearer ${token}`
    }
    return config
  },
  error => {
    return Promise.reject(error)
  }
)

// 响应拦截器
api.interceptors.response.use(
  response => {
    const res = response.data
    if (res.code !== 200) {
      Message.error(res.message || '请求失败')
      return Promise.reject(new Error(res.message || '请求失败'))
    }
    return res
  },
  error => {
    Message.error(error.message || '网络错误')
    return Promise.reject(error)
  }
)

export default {
  // 用户相关
  login: (data) => api.post('/auth/login', data),
  register: (data) => api.post('/user/registeredUser', data),
  getUserInfo: () => api.get('/user/username'),
  updateProfile: (data) => api.put('/user', data),
  
  // 职位相关
  getJobs: (params) => api.post(`/employment/${params.pageNum}/${params.pageSize}`, params.filters),
  getJobDetail: (id) => api.get(`/employment/${id}`),
  applyJob: (data) => api.post('/employmentUser', data),
  
  // 企业相关
  getCompanies: (params) => api.post(`/company/${params.pageNum}/${params.pageSize}`, params.filters),
  getCompanyDetail: (id) => api.get(`/company/${id}`),
  registerCompany: (data) => api.post('/company', data),
  
  // 简历相关
  uploadResume: (file) => {
    const formData = new FormData()
    formData.append('file', file)
    return api.post('/resume/upload', formData, {
      headers: { 'Content-Type': 'multipart/form-data' }
    })
  },
  getMyResumes: (pageNum, pageSize) => api.get(`/resume/user/${pageNum}/${pageSize}`),
  deleteResume: (id) => api.delete(`/resume/${id}`)
}
```

### 工具函数 (utils/auth.js)
```javascript
const TOKEN_KEY = 'token'
const USER_INFO_KEY = 'userInfo'

export function getToken() {
  return localStorage.getItem(TOKEN_KEY)
}

export function setToken(token) {
  localStorage.setItem(TOKEN_KEY, token)
}

export function removeToken() {
  localStorage.removeItem(TOKEN_KEY)
}

export function getUserInfo() {
  const userInfo = localStorage.getItem(USER_INFO_KEY)
  return userInfo ? JSON.parse(userInfo) : null
}

export function setUserInfo(userInfo) {
  localStorage.setItem(USER_INFO_KEY, JSON.stringify(userInfo))
}

export function removeUserInfo() {
  localStorage.removeItem(USER_INFO_KEY)
}

export function isLoggedIn() {
  return !!getToken()
}

export function isEnterpriseUser() {
  const userInfo = getUserInfo()
  return userInfo && userInfo.role === 'Enterprise_User'
}

export function logout() {
  removeToken()
  removeUserInfo()
  window.location.href = '/login'
}
```

## 公共组件

### 布局组件 (Layout.vue)
```vue
<template>
  <div class="layout">
    <el-header class="header">
      <div class="logo">校园招聘系统</div>
      <el-menu mode="horizontal" class="nav-menu">
        <el-menu-item index="1">首页</el-menu-item>
        <el-menu-item index="2">职位</el-menu-item>
        <el-menu-item index="3">企业</el-menu-item>
        <el-menu-item index="4">文章</el-menu-item>
      </el-menu>
      <div class="user-info">
        <el-dropdown v-if="isLoggedIn">
          <span class="el-dropdown-link">
            {{ userInfo.nickName }}
            <i class="el-icon-arrow-down"></i>
          </span>
          <el-dropdown-menu slot="dropdown">
            <el-dropdown-item>个人中心</el-dropdown-item>
            <el-dropdown-item @click.native="logout">退出登录</el-dropdown-item>
          </el-dropdown-menu>
        </el-dropdown>
        <div v-else>
          <el-button type="text" @click="$router.push('/login')">登录</el-button>
          <el-button type="primary" @click="$router.push('/register')">注册</el-button>
        </div>
      </div>
    </el-header>
    <el-main class="main">
      <router-view />
    </el-main>
  </div>
</template>
```

### 分页组件 (Pagination.vue)
```vue
<template>
  <div class="pagination-wrapper">
    <el-pagination
      @size-change="handleSizeChange"
      @current-change="handleCurrentChange"
      :current-page="currentPage"
      :page-sizes="[10, 20, 50, 100]"
      :page-size="pageSize"
      layout="total, sizes, prev, pager, next, jumper"
      :total="total">
    </el-pagination>
  </div>
</template>

<script>
export default {
  name: 'Pagination',
  props: {
    total: {
      required: true,
      type: Number
    },
    page: {
      type: Number,
      default: 1
    },
    limit: {
      type: Number,
      default: 20
    }
  },
  computed: {
    currentPage: {
      get() {
        return this.page
      },
      set(val) {
        this.$emit('update:page', val)
      }
    },
    pageSize: {
      get() {
        return this.limit
      },
      set(val) {
        this.$emit('update:limit', val)
      }
    }
  },
  methods: {
    handleSizeChange(val) {
      this.$emit('pagination', { page: this.currentPage, limit: val })
    },
    handleCurrentChange(val) {
      this.$emit('pagination', { page: val, limit: this.pageSize })
    }
  }
}
</script>
```

## 部署配置

### 开发环境配置
```javascript
// vue.config.js
module.exports = {
  devServer: {
    port: 8080,
    proxy: {
      '/api': {
        target: 'http://localhost:3030',
        changeOrigin: true,
        pathRewrite: {
          '^/api': ''
        }
      }
    }
  }
}
```

### 生产环境配置
```javascript
// 生产环境API地址配置
const API_BASE_URL = process.env.NODE_ENV === 'production' 
  ? 'https://your-domain.com/api' 
  : 'http://localhost:3030'
```

## 注意事项

1. **权限控制**: 前端路由需要配合后端JWT认证
2. **跨域处理**: 开发环境使用代理，生产环境需要后端配置CORS
3. **文件上传**: 简历上传需要使用FormData格式
4. **状态管理**: 用户信息和Token需要持久化存储
5. **错误处理**: 统一的错误提示和处理机制
6. **响应式设计**: 适配不同屏幕尺寸的设备